{
  "name": "pokebattle-api",
  "version": "1.0.0",
  "description": "an api that allowes the user to play pokemon battle using API requests",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.1.4",
    "express": "^4.18.2",
    "mongodb": "^5.6.0"
  }
}
